require "spec_helper"

RSpec.describe Youdao::Rb do
  it "has a version number" do
    expect(Youdao::Rb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
